{
  "name": "@speckle/bundle-spec",
  "version": "1.0.0",
  "description": "Single source of truth for the Speckle bundle format (eav + envelope + geometry). Schema and semantics live in spec/bundle-spec.sql; everything else is generated.",
  "type": "module",
  "main": "generated/ts/bundleSpec.ts",
  "files": [
    "spec/",
    "generated/ts/",
    "conformance/"
  ],
  "scripts": {
    "generate": "node codegen/generate-all.mjs",
    "publish:artifacts": "node codegen/publish.mjs",
    "verify-pin": "node codegen/verify-pin.mjs",
    "validate": "node validator/validate-bundle.mjs",
    "build:query-fixture": "node codegen/build-query-fixture.mjs",
    "test": "node tests/conformance/run.mjs && node tests/validator/run.mjs && node tests/query-conformance/run.mjs",
    "check": "node codegen/generate-all.mjs && git diff --exit-code generated docs/reference.md"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "Apache-2.0"
}
