node_modules/
*.log
.DS_Store
# publish artifacts (built by `npm run publish:artifacts`; the lockfile is the pin)
dist/
# generated/ is COMMITTED (vendored by consumers) — do not ignore it.

# Python
__pycache__/
*.pyc
